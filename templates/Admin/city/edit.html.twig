{% trans_default_domain "OurTripsTrans" %}
{% extends 'base.html.twig' %}
{% block title %}{{ "title.city.edit"|trans({"%name%": city.name}) }}{% endblock %}

{% block body %}
    <h1>{{ "title.city.edit"|trans({"%name%": city.name}) }}</h1>

    {{ include('Admin/city/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('admin_city_index') }}"><button class="warning"><i class="fas fa-arrow-alt-circle-left"></i></button></a>

    {{ include('Admin/city/_delete_form.html.twig') }}
{% endblock %}
